<h1>Productos sin sockets:</h1>
<ul>
    {{#if products}}
        {{#each products}}
            <li>{{this.title}} - ${{this.price}}</li>
        {{/each}}
    {{else}}
        <p>No hay productos</p>
    {{/if}}
</ul>